import FacebookSvg from './facebook.svg';
import React from 'react';

const FacebookIcon: React.FunctionComponent = (props) => (
  <img {...props} src={FacebookSvg} />
);

export default FacebookIcon;
